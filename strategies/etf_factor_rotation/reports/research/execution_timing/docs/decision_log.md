# 决策日志

- `2026-05-18`: 先补本地底座，再做执行延迟与信号刷新拆分；真实模拟盘口径未确认前，不提前选定 `logic-2` 或 `logic-3`。
- `2026-05-18`: 刷新带 `open` 的行情包后，baseline 本地复算最大最终权重误差收敛到约 `3.23e-09`；本地三组时序路径已可解释。
- `2026-05-18`: 第一轮有效本地结果触发云端升级条件，原因是 `logic-2` / `logic-3` 最大回撤均明显恶化，且 `signal_shift` 不是罕见事件。
- `2026-05-18`: 第一版云端批次发现长假周实现缺口，`logic-2` / `logic-3` 仅覆盖 `269/272` 次周信号；该批次保留为诊断记录，不作为结论来源。
- `2026-05-18`: 修复为队列执行后，第二版云端批次完整覆盖 `272/272` 次周信号；正式结果显示 `logic-2` 最大回撤恶化 `1.13pp`，`logic-3` 最大回撤恶化 `3.44pp`，时序差异不可忽略。
- `2026-05-18`: 在真实人工执行口径尚未确认前，不把 `logic-2` 或 `logic-3` 写回默认；当前结论是“正式差异已确认，主基线选择待真实流程确认”。
