subject:
  identity_fields:
  - context.repo
  - context.worktree
  - context.branch
  required_fields:
  - context.repo
  - context.worktree
  - context.branch
  context_sources:
  - context
  - subject
  - event
  existing_match_policy: exact
  create_policy: explicit_activation_only
  ambiguous_policy: audit
