#!/bin/sh
set -eu

if command -v pre-commit >/dev/null 2>&1; then
  pre-commit run --hook-stage pre-commit
fi

sh .githooks/run-python.sh \
  -m scripts.research.governance.pr_flow intent pre-commit

sh .githooks/run-python.sh \
  -m scripts.research.governance verify fast --staged
